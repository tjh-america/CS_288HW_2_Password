#A bash script that prompts the user for an ID, checks if it is in /etc/passwd.
#If the ID is in the passwd file, asks the user if this is the entry that should be changed
#If it is, prompt for new login shell and create a file named passwd.1 which substitues old login shell w/ new one.
